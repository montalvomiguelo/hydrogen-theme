{%- render 'heading',
  content: block.settings.title,
  level: 'h2',
  size: 'md',
  class: 'px-6 md:px-8 lg:px-12',
  shopify_attributes: block.shopify_attributes
-%}

{% schema %}
{
  "name": "Header",
  "tag": null,
  "settings": [
    {
      "type": "inline_richtext",
      "id": "title",
      "label": "Heading",
      "default": "Featured collection"
    }
  ]
}
{% endschema %}
