{%- render 'card-product', card_product: block.settings.product, wrapper_class: 'w-80' -%}

{% schema %}
{
  "name": "Product card",
  "tag": null,
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    }
  ],
  "presets": [
    {
      "name": "Product card",
      "category": "Product",
      "settings": {
        "product": "{{ closest.product }}"
      }
    }
  ]
}
{% endschema %}
